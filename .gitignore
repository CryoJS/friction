node_modules/
dist/
coverage/
*.log
*.tsbuildinfo
.DS_Store

# secrets
.env
.env.local
.dev.vars

# Cloudflare local state (local D1 + R2 live here; keep it out of git)
.wrangler/

# Stagehand caches / inference logs
.stagehand/
inference_summary/

<THEIRS>
.claude/worktrees/

# The GitHub connection made with the Connect button: a token. Never commit it.
.friction/
