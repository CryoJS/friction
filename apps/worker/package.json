{
  "name": "@friction/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply friction --local",
    "db:migrate:remote": "wrangler d1 migrations apply friction --remote",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@friction/shared": "workspace:*",
    "hono": "^4.13.8",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260907.1",
    "typescript": "^7.0.2",
    "wrangler": "4.129.1"
  }
}
